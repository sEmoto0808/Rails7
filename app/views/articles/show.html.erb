<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= button_to "Delete Article", { id: @article.id }, method: :delete %>
</ul>

<!--
  パーシャルを使ってリファクタリング
  renderメソッドが@article.commentsコレクションに含まれる要素を1つ1つ列挙するときに、
  各コメントをパーシャルと同じ名前のローカル変数に自動的に割り当てます。
  この場合はcommentというローカル変数が使われ、パーシャルの表示に利用されます
-->
<h2>Comments</h2>
<%= render @article.comments %>

<!-- パーシャルを使ってリファクタリング -->
<h2>Add a comment:</h2>
<%= render 'comments/form' %>
